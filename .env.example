# ===========================================
# LiveCity — Переменные окружения
# ===========================================
# Скопируйте этот файл: cp .env.example .env.local
# Заполните значения и НИКОГДА не коммитьте .env.local

# --- Database (PostgreSQL + PostGIS) ---
# Формат: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Neon: https://console.neon.tech → создать проект → скопировать connection string
DATABASE_URL=postgresql://user:password@localhost:5432/livecity

# --- Google Gemini AI ---
# Получить ключ: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# --- Google Places API (сбор реальных отзывов) ---
# Получить ключ: https://console.cloud.google.com/apis/credentials
# Включить: Places API в Google Cloud Console
# Бесплатный лимит: $200/мес (~11,700 запросов Place Details)
GOOGLE_PLACES_API_KEY=your_google_places_api_key_here

# --- Apify (парсинг 2GIS и Instagram) ---
# Регистрация: https://apify.com → Get free $5 credit
# Получить токен: https://console.apify.com/account/integrations
#
# Instagram: apify/instagram-scraper (ID: shu8hvrXbJbY3Eb9W)
#   Цена: от $1.50/1000 результатов
#   Поля: directUrls, resultsType=posts, resultsLimit, onlyPostsNewerThan
#   Выход: caption, likesCount, commentsCount, timestamp, ownerUsername
#
# 2GIS: найти актор "2GIS Scraper" на Apify Store (Outscraper)
#   Поля: startUrls, maxReviews, language=ru
#   Выход: text, rating, author, date
#
# APIFY_TOKEN=your_apify_token_here
# APIFY_2GIS_ACTOR=actor_id_for_2gis_scraper
# APIFY_INSTA_ACTOR=apify/instagram-scraper

# --- Cron (защита эндпоинтов обновления) ---
# sync-pulse: POST /api/cron/sync-pulse (основной пайплайн, каждые 6ч)
# refresh-scores: POST /api/cron/refresh-scores (пересчёт скоров, каждые 15мин)
CRON_SECRET=your_cron_secret_here

# --- Mapbox ---
# Получить токен: https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_TOKEN=your_mapbox_token_here

# --- App Settings ---
# Координаты центра карты по умолчанию (Алматы)
NEXT_PUBLIC_DEFAULT_LAT=43.2380
NEXT_PUBLIC_DEFAULT_LNG=76.9450
NEXT_PUBLIC_DEFAULT_ZOOM=12
